<div class="col-md-4">

    {{#each imgs}}
        <img src="/images/uploads/{{this}}" class="img-responsive img-thumbnail" />
    {{else}}
        <div class="col-md-12 text-center">
            <strong>No Images Available</strong>
        </div>
    {{/each}}
    
</div>