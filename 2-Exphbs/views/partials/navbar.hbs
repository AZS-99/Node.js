<div class="flex-between span-all-cols bg-grey">
    <ul class="flex gap-1rem no-bullets">
        {{#each left_nav}}
            <li><a href={{this}}>{{@key}}</a></li>
        {{/each}}
    </ul>

    <ul class="flex gap-1rem no-bullets">
        {{#each right_nav}}
            <li><a href={{this}}>{{@key}}</a></li>
        {{/each}}
    </ul>
</div>